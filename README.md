# Somnium test task

## Installation

- `$ git clone https://github.com/kaliyev-dev/somnium-test-task.git`
- `$ cd somnium-test-task`
- `$ yarn` init packages

## Usage

- `$ yarn start` starts dev server
- `$ yarn build` builds prod version of app into `build/`
